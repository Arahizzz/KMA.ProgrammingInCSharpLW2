<Window x:Class="LW5Polishchuk.Views.ModulesList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:LW5Polishchuk.Views"
        mc:Ignorable="d"
        Title="ModulesList" Height="450" Width="800">
    <Grid>
        <DataGrid 
            ItemsSource="{Binding Processes, NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged}">
            <DataGridTextColumn Header="Name" Width="200" Binding="{Binding ModuleName}" />
            <DataGridTextColumn Header="Address" Width="50" Binding="{Binding BaseAdress}" />
            <DataGridTextColumn Header="File Name" Width="50" Binding="{Binding FileName}" />
        </DataGrid>
    </Grid>
</Window>

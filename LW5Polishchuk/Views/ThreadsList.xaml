<Window x:Class="LW5Polishchuk.Views.ThreadsList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:LW5Polishchuk.Views"
        mc:Ignorable="d"
        Title="ThreadsList" Height="450" Width="800">
    <Grid>
        <DataGrid ItemsSource="{Binding ProcessThreads, NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Width="200" Binding="{Binding Id}" />
                <DataGridTextColumn Header="State" Width="50" Binding="{Binding ThreadState}" />
                <DataGridTextColumn Header="Start Time" Width="50" Binding="{Binding StartTime, IsAsync=True}" />
                <DataGridTextColumn Header="CPU Time" Width="50" Binding="{Binding TotalProcessorTime, IsAsync=True}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>